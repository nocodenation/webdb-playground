# WebDB Playground

1. Copy `.env.example` to `.env`
2. Run with `./start.sh`
3. Access services at:
   - pgAdmin: http://localhost:8100/
   - REST: http://localhost:8101/
   - Swagger: http://localhost:8102/

Run `./stop.sh` to stop.
